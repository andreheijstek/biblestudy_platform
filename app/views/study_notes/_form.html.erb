<%= simple_form_for(study_note) do |f| %>
    <%= f.input :starting_verse %>
    <%= f.input :ending_verse %>
    <%= f.input :study_note %>
    <%= f.button :submit, class: "btn-primary" %>
<% end %>
