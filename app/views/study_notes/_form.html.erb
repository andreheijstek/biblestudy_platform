<%= simple_form_for (study_note) do |s|  %>
    <%= s.simple_fields_for :pericope do |p| %>
        <%= p.input :name %>
    <% end %>
    <%= s.input :title %>
    <%= s.input :note %>
    <%= s.button :submit, class: "btn-primary" %>
<% end %>
