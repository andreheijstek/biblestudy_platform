<%= simple_form_for (studynote) do |s|  %>
    <%= s.simple_fields_for :pericopes do |p| %>
        <%= p.input :name %>
    <% end %>
    <%= s.input :title %>
    <%= s.input :note %>
    <%= s.button :submit, class: "btn-primary" %>
<% end %>
