= simple_form_for studynote, wrapper: :vertical_input_group, validate_pericope_order: true do |f|
  #pericopes
    = f.simple_fields_for :pericopes, wrapper: :vertical_input_group do |pericope|
      = render 'pericope_fields', f: pericope
    .links
      = link_to_add_association t('add_pericope'), f, :pericopes, class: 'new btn-xs', id: 'add_pericope'
  = f.input :title
  = f.input :note, as: :trix_editor, id: 'editor', class: 'editor'
  = f.button :submit, class: "btn-primary", id: 'submit_form'
