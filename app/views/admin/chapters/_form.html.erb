<%= simple_form_for([:admin, biblebook, chapter]) do |f| %>
    <%= f.input :chapter_number %>
    <%= f.input :description %>
    <%= f.input :nrofverses %>
    <%= f.button :submit, class: "btn-primary" %>
<% end %>
